export interface FunnelMetrics {
  targetCPA: number;
  conversionRate: number;
  maxCPC: number;
}

export interface SimulationData {
  clicks: number;
  cost: number;
  conversions: number;
  cpa: number;
}
